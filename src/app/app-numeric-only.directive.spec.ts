import { AppNumericOnlyDirective } from './app-numeric-only.directive';

describe('AppNumericOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNumericOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
