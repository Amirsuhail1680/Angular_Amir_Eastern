import { AlpahbetOnlyDirective } from './alpahbet-only.directive';

describe('AlpahbetOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlpahbetOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
